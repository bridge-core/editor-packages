{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"type": "object",
	"properties": {
		"condition": {
			"title": "Match Tool",
			"description": "Checks tool used to mine the block. Requires tool provided by loot context, and always fails if not provided.",
			"const": "minecraft:match_tool"
		},
		"predicate": {
			"title": "Predicate",
			"description": "Predicate applied to item.",
			"$ref": "../../general/itemProperties.json"
		}
	}
}
