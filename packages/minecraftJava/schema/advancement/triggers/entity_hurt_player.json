{
	"$schema": "http://json-schema.org/draft-07/schema",
	"type": "object",
	"properties": {
		"trigger": {
			"title": "Trigger: Entity Hurt Player",
			"description": "Triggers after a player gets hurt (even when it's not caused by an entity)",
			"const": "minecraft:entity_hurt_player"
		},
		"conditions": {
			"title": "Conditions",
			"description": "Object containing conditions that need to be met when the trigger gets activated. The criterion is marked completed when the trigger activates and all conditions passed.",
			"type": "object",
			"properties": {
				"player": {
					"$ref": "../general/player.json"
				},
				"damage": {
					"title": "Damage",
					"description": "Checks the damage done to the player.",
					"$ref": "../../general/damageProperties.json"
				}
			}
		}
	}
}
