{
	"$schema": "http://json-schema.org/draft-07/schema",
	"title": "Requirements",
	"description": "Defines how these criteria above are completed to grant the advancement. Contains sublists, which in turn contain names of criteria from this advancement. When a criterion is newly completed or revoked, the advancement is granted if all sublists have at least one criterion within them completed. Optional, defaults to requiring all criteria completed.",
	"type": "array",
	"items": {
		"type": "array",
		"items": {
			"type": "string",
			"enum": [] // TODO: Criterion schema scripts
		}
	}
}
