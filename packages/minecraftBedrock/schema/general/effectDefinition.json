{
	"$schema": "http://json-schema.org/draft-07/schema",
	"type": "object",
	"description": "Defines an effect to add to the entity",
	"properties": {
		"effect": {
			"$ref": "./effectName.json"
		},
		"duration": {
			"$ref": "./effectDuration.json",
			"description": "The time duration in seconds of the effect"
		},
		"amplifier": {
			"type": "integer",
			"description": "The amplifier for the effect"
		},
		"visible": {
			"type": "boolean",
			"description": "Whether to show effect particles"
		},
		"display_on_screen_animation": {
			"type": "boolean",
			"description": "Whether to show an on screen effect icon animation upon applying the effect"
		}
	}
}
