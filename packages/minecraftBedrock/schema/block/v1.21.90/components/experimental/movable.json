{
    "$schema": "https://json-schema.org/draft-07/schema",
    "title": "Movable",
    "description": "The description identifier of the movable component",
    "type": "object",
    "properties": {
        "movement_type": {
            "title": "Movement Type",
            "description": "How the block reacts to being pushed by another block like a piston.",
            "type": "string",
            "enum": [
                "push_pull",
                "push",
                "popped",
                "immovable"
            ]
        },
        "sticky": {
            "title": "Sticky",
            "description": "How the block should handle adjacent blocks around it when being pushed by another block like a piston.",
            "type": "string",
            "enum": [
                "same",
                "none"
            ]
        }
    }
}