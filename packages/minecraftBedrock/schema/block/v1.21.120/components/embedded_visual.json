{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "Embedded Visual",
    "description": "Defines which geometry and material_instances to use when the block is embedded into another block, eg. into a Flowerpot",
    "type": "object",
    "properties": {
        "geometry": {
            "description": "The 'minecraft:geometry' component that will be used for the embedded visual.",
            "$ref": "../../v1.21.100/components/geometry.json"
        },
        "material_instances": {
            "description": "The 'minecraft:material_instances' component that will be used for the embedded visual.",
            "$ref": "../../v1.21.80/components/material_instances.json"
        }
    }
}