{
	"$schema": "http://json-schema.org/draft-07/schema",
	"additionalProperties": false,
	"description": "Event to run when a raid is triggered on the village.",
	"type": "object",
	"title": "Raid Trigger",
	"properties": {
		"triggered_event": {
			"title": "Event: On Raid Trigger",
			"description": "Event to run we attempt to trigger a raid on the village.",
			"$ref": "../../general/v1.16.0/eventDefinition.json"
		}
	}
}
