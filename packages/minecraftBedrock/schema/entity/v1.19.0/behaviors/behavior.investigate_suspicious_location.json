{
    "$schema": "http://json-schema.org/draft-07/schema",
    "type": "object",
    "additionalProperties": false,
    "description": "The entity moves towards a 'suspicious' position based on data gathered in minecraft:suspect_tracking",
    "properties": {
        "priority": { "$ref": "../../general/behaviorPriority.json" },
        "goal_radius": {
            "description": "Distance in blocks within the entity considers it has reached it's target position.",
            "type": "number"
        },
        "speed_multiplier": {
            "description": "Movement speed multiplier.",
            "type": "number"
        }
    }
}