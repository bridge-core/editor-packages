{
	"type": "enum",
	"generateFile": "lootTable/dynamic/shortLootTablePathEnum.json",
	"script": "return readdir('BP/loot_tables').then(dirents => dirents.map(dirent => `\"${dirent.path.replace('BP/loot_tables/', '').replace('.json', '')}\"`))"
}
